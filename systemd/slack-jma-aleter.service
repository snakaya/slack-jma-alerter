[Unit]
Description=slack-jma-aleter
After=syslog.target network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/node /path/to/slack-jma-aleter/jmapull.js  # インストール先ディレクトリへ変更してください
WorkingDirectory=/path/to/slack-jma-aleter                          # インストール先ディレクトリへ変更してください
KillMode=process
Restart=always
User=www-data                                                       # 実行ユーザー名へ変更してください
Group=www-data                                                      # 実行ユーザーのグループ名へ変更してください

[Install]
WantedBy=multi-user.target